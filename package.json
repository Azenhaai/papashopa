{
  "name": "papashopa",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run export:app && astro dev",
    "build": "npm run export:app && astro build",
    "export:app": "node scripts/export_app_data.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
